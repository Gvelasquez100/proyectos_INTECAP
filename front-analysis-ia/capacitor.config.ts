import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'umg.analysis.edu',
  appName: 'front-analysis-ia',
  webDir: 'dist/front-analysis-ia',
  server: {
    androidScheme: 'https'
  }
};

export default config;
